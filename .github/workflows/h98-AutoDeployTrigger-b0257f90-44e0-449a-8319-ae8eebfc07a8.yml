name: Trigger auto deployment for h98

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ docs-demo ]
    paths:
    - '//**'
    - '.github/workflows/h98-AutoDeployTrigger-b0257f90-44e0-449a-8319-ae8eebfc07a8.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.H98_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.H98_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.H98_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryslkvv4wlgfp34.azurecr.io
          registryUsername: ${{ secrets.H98_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.H98_REGISTRY_PASSWORD }}
          containerAppName: h98
          resourceGroup: nextjs14a96d
          imageToBuild: containerregistryslkvv4wlgfp34.azurecr.io/h98:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


